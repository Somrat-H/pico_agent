import 'package:flutter/material.dart';

class RemoveDoctor extends StatelessWidget {
  const RemoveDoctor({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Remove Doctor"),
    );
  }
}